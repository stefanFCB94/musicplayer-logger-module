language: node_js
sudo: false
node_js:
  - "10"
install:
  - npm install
script:
  - npm run lint && npm test
before_deploy:
  - npm install && npm run build
deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    tags: true

